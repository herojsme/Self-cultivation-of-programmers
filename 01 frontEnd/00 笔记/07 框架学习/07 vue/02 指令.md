<!--  vue -->

<!--
    指令
        v-bind 绑定
            简写 v-bind:id =:id
            动态绑定多个值 可以传递一个对象 v-bind="objectOfAttrs"  objectOfAttrs = {id: 'container',class: 'wrapper'}
            动态参数  :[attributeName]="url"
        v-html 插入 HTML
        v-on
            简写 v-on:click =@click
            @[eventName]="doSomething"
            可以有.开头的修饰符 modifier
               .once .stop .prevent .enter
        v-for
            v-for="(item, index) in items" v-for="{ message } in items"
            v-if 比 v-for 的优先级更高
            注意key值
        v-if  v-else-if  v-else
        v-show 切换display
        v-model 表单 .lazy .number .trim
        自定义指令：
            <script setup>v 开头的驼峰式命名的变量都可以被用作一个自定义指令，
            组件式中用directives 注册
            全局中app.directive()
 -->
