老师，点击按钮 new 出来一个我自己写的对象，要怎么在创建它的作用域外删除它呢，比如 btn.onclick = () => { let div1 = new Box() }；我要在 btn2.onclick 里面去删除它
删除

let div1=null;
btn1.onclick=function (){
  let div1=new Box();
};

btn2.onclick=function (){
  btn1.div1
};



前台内存泄漏怎么看
  高级语言——没有

react 高阶组件能说下吗?
High0rder Component
Factory——工厂

-------------------------------------------------------------------------------------------------------------------

前端里的  组件和middle ware 什么区别
目的：简单编写、修改程序
重用：
1.函数
2.goto
3.class
4.组件
5.模块
6.middle Ware

1.叫法不同
2.每个方法之间的区别，其实不大

-------------------------------------------------------------------------------------------------------------------

组件、middleware
1.出发点不同
  组件——拿过来就用
  middleware——屏蔽底层的区别

NodeJS的KOA

-------------------------------------------------------------------------------------------------------------------

微信——API
1.调用本地接口——拍照
2.文件操作
3.上传服务器

-------------------------------------------------------------------------------------------------------------------

wx的for
v-for="a,index in arr"
  {{a}}
  {{index}}

  :key="index"

wx:for="{{arr}}"
  {{item}}
  {{index}}

  wx:key="{{xxx}}"

  改名：
    wx:for-item="名字"
    wx:for-index="名字"

1.输出数据(innerHTML、属性)
  <view>我叫：{{name}}</view>
  <view wx:for="{{arr}}"></view>

2.wx指令
  wx:xxx=""

3.设置数据
  this.setData({});		类似于react的setState({})

-------------------------------------------------------------------------------------------------------------------

数据通信
wx.request
wx.uploadFile

-------------------------------------------------------------------------------------------------------------------

老师,我想用小程序做个类似优酷的应用,底部的tabbar我知道跳转页面,可是页面中的标题切换就出来新页面或者左右滑动也可以,这个不是路由跳转吧,给点思路,还有小程序中的组件

老师，有机会讲下小程序唤醒app及app跳转小程序的实现，有点晕。

-------------------------------------------------------------------------------------------------------------------

<scroll-view scroll-x="{{true}}">
</scroll-view>

-------------------------------------------------------------------------------------------------------------------

微信会限制通信地址
https://www.abc.com/

-------------------------------------------------------------------------------------------------------------------

微信小程序测试——
后台 -> 添加开发者 -> 你的微信 -> 扫码

-------------------------------------------------------------------------------------------------------------------

API挺统一：
wx.xxxx({
  参数...,
  success(res){
    res.data/result/...
  },
  fail(err){
    err.xxx
  }
});

-------------------------------------------------------------------------------------------------------------------

wx.request({
  url,
  dataType,
  success, fail
});

-------------------------------------------------------------------------------------------------------------------

在哪里全局配置服务器地址

做一下微信授权认证和支付吧
  登录->配合后台

-------------------------------------------------------------------------------------------------------------------

微信组件
